const iconDefaultSize = "16px";
export function RefreshOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";

  return (
    <div className="material-symbols-outlined" style={{ fontSize: size, verticalAlign: "middle", color }}>
      refresh
    </div>
  );
}

export function PlannerOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";
  return (
    <div className="material-symbols-outlined" style={{ fontSize: size, verticalAlign: "middle", color: color }}>
      planner_banner_ad_pt
    </div>
  );
}

export function SmartToyOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";

  return (
    <div className="material-symbols-outlined" style={{ fontSize: size, verticalAlign: "middle", color }}>
      smart_toy
    </div>
  );
}

export function StarsOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";

  return (
    <div className="material-symbols-outlined" style={{ fontSize: size, verticalAlign: "middle", color }}>
      stars
    </div>
  );
}

export function CloudDownloadOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";

  return (
    <div className="material-symbols-outlined" style={{ fontSize: size, verticalAlign: "middle", color }}>
      cloud_download
    </div>
  );
}

export function VerifiedOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";
  var fontVariationSettings = `'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 48`;

  return (
    <div
      className="material-symbols-outlined"
      style={{ fontSize: size, verticalAlign: "middle", color, fontVariationSettings }}
    >
      verified
    </div>
  );
}

export function GradeOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";
  var fontVariationSettings = `'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 48`;

  return (
    <div
      className="material-symbols-outlined"
      style={{ fontSize: size, verticalAlign: "middle", color, fontVariationSettings }}
    >
      grade
    </div>
  );
}

export function SendOutlined(props: any) {
  var size = props.fontSize || iconDefaultSize;
  var color = props.color || "black";
  return (
    <div className="material-symbols-outlined" style={{ fontSize: size, verticalAlign: "middle", color: color }}>
      send
    </div>
  );
}
